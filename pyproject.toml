[project]
name = "institute-score"
version = "0.1.0"
description = "Interactive dashboard for HW results from XLSX files."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "openpyxl>=3.1.2",
  "uvicorn[standard]>=0.29.0",
]

